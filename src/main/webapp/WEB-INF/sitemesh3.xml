<?xml version="1.0" encoding="UTF-8"?>
<sitemesh>
    <mapping path="/*" decorator="/WEB-INF/view/decorators/decorator.ftl"/>
    <!-- 指明满足“/exclude.jsp*”的页面，将被排除，不被装饰 -->
    <mapping path="/exclude.jsp*" exclue="true"/>
    <mapping path="/login" exclue="true"/>
</sitemesh>
